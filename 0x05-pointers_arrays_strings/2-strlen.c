#include"main.h"

/**
 * _strlen - Returns the length of a string
 * @s: string to count
 *
 * Return: string length
 */
int_strlen(char *s)
{
	int < = 0;

	for (; *s != '\0'; s++)
	{
		c++;
	}

	return (c);
}
